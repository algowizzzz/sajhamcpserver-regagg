{
  "name": "corpus_read_many",
  "implementation": "sajha.tools.impl.corpus_tools.CorpusReadManyTool",
  "description": "Read the opening of several documents in one call, for comparison. Each result reports total_chars and whether it was truncated \u2014 use corpus_read with offset to read any of them in full.",
  "version": "1.0.0",
  "enabled": true,
  "inputSchema": {
    "type": "object",
    "properties": {
      "doc_ids": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "max_chars_each": {
        "type": "integer",
        "default": 12000,
        "description": "characters per document"
      }
    },
    "required": [
      "doc_ids"
    ]
  },
  "outputSchema": {
    "type": "object"
  },
  "metadata": {
    "author": "regagg",
    "category": "Corpus Retrieval",
    "tags": [
      "corpus",
      "markdown",
      "retrieval",
      "riskgpt"
    ],
    "cacheTTL": 120,
    "readOnly": true
  }
}
